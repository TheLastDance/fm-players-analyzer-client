{
  "name": "ts-express-react-vite-template",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-server": "concurrently \"npm run start-server:server\" ",
    "start-client": "concurrently \"npm run start-client:client\" ",
    "build": "concurrently \"npm run build:build \" ",
    "install-server": "cd server && npm i",
    "install-client": "cd client && npm i",
    "start-server:server": "cd server && npm start",
    "start-client:client": "cd client && npm run dev",
    "build:build": "cd client && npm run build"
  },
  "author": "TheLastDance",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "concurrently": "^8.2.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  }
}